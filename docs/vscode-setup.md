# VS Code Setup moved

The LLM-facing VS Code setup reference now lives at:

- [`../skills/install-spec-loop/docs/vscode-setup.md`](../skills/install-spec-loop/docs/vscode-setup.md)

If you are following older setup instructions, prefer asking the LLM to run
`install-spec-loop` instead of applying editor setup manually.
